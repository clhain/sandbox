name: Porter CNAB Tag Version

on: workflow_call

jobs:
  publish:
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v2
      - name: install yq
        run: |
          sudo snap install yq
      - name: Set Environment Variables
        run: |
          OWNER="$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')"
          echo "REPO-OWNER=${OWNER}" >> $GITHUB_ENV
          BUNDLE-NAME="$(yq e '.name' ./bundle/porter.yaml )"
          echo "BUNDLE-NAME=${BUNDLE-NAME}" >> $GITHUB_ENV
          BUNDLE-VERSION="$(yq e '.version' ./bundle/porter.yaml )"
          echo "BUNDLE-VERSION=${BUNDLE-VERSION}" >> $GITHUB_ENV
      - name: Login to GitHub Packages OCI Registry
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9 # docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Tag bundle image
        run: |
          docker tag ghcr.io/${{ env.REPO-OWNER }}/${{env.BUNDLE-NAME}}:${{env.BUNDLE-VERSION}}-rc ghcr.io/${{ env.REPO-OWNER }}/${{env.BUNDLE-NAME}}:${{env.BUNDLE-VERSION}}